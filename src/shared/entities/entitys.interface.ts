export interface ImgsConfig {
  src: string;
  alt: string;
}

export interface LinkConfig {
  href: string;
  label: string;
}

export interface InputConfig {
  type: string;
  placeholder: string;
}
